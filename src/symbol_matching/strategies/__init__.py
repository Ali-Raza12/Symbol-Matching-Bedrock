"""Per-strategy matchers.

Each module exposes a `find_matches(page_image, templates_or_reference, ...) -> list[MatchResult]`
shape, so notebooks 01–05 can swap between them with the same calling code.
"""
